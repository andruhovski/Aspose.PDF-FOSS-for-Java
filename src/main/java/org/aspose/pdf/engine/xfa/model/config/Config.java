package org.aspose.pdf.engine.xfa.model.config;

import org.aspose.pdf.engine.xfa.model.XfaNode;
import org.w3c.dom.Element;

/// Typed XFA template element `config`. Attribute and child
/// accessors are typed; unknown attributes/children round-trip via the DOM.
public final class Config extends XfaNode {

    /// Wraps a backing `config` element.
    public Config(Element element, XfaNode parent) {
        super(element, parent);
    }
}
