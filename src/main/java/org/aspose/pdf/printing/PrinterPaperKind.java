package org.aspose.pdf.printing;

/**
 * Standard paper sizes.
 */
public enum PrinterPaperKind {
    /** Custom paper size. */
    Custom,
    /** US Letter (8.5 x 11 in). */
    Letter,
    /** US Legal (8.5 x 14 in). */
    Legal,
    /** A3 (297 x 420 mm). */
    A3,
    /** A4 (210 x 297 mm). */
    A4,
    /** A5 (148 x 210 mm). */
    A5,
    /** B4 (250 x 353 mm). */
    B4,
    /** B5 (176 x 250 mm). */
    B5,
    /** Tabloid (11 x 17 in). */
    Tabloid,
    /** Ledger (17 x 11 in). */
    Ledger,
    /** Executive (7.25 x 10.5 in). */
    Executive
}
